import fs from 'node:fs';import {spawnSync} from 'node:child_process';const d='examples/full-robot/gait-exploration';let names=['opposite-pair-50mm','opposite-pair-100mm','opposite-pair-clear-flight','long-stride-wave-compact-support','continuous-wave-compact-support'],status=[];for(let name of names){let o=fs.openSync(`${d}/${name}.plan.json`,'wx'),e=fs.openSync(`${d}/${name}.plan-error.txt`,'wx'),start=performance.now();let r=spawnSync('/Users/elliot/physics-simulator/target/gait-exploration/release/examples/plan_marker_motion',['examples/full-robot/whole-swing/settled-integral-candidate.scene.json',`${d}/workspace-markers.json`,`${d}/${name}.planning.json`],{stdio:['ignore',o,e],timeout:60000});fs.closeSync(o);fs.closeSync(e);status.push({name,exit_status:r.status,error:r.error?.message??null,wall_s:(performance.now()-start)/1000});fs.writeFileSync(`${d}/followup-planning-status.json`,JSON.stringify(status,null,2)+'\n');console.log(status.at(-1));if(r.error)break}
