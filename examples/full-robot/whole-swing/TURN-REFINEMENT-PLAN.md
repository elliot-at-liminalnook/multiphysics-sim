# Refine the passing steering posture

The -24 mm neutral front support passes all 15 swings and endpoint checks for
the 20 ms student and 5 ms teacher. The -27 mm alternative collides at 9.4 s
in both profiles. Preserve that failed boundary rather than loosening geometry.

Repeat the exact passing mixed-input student at 5 ms and teacher at 2.5 ms.
All other parameters, actions, task and seed remain unchanged. Compare the
student 20/5 ms and teacher 5/2.5 ms trajectories at common 20 ms report times,
requiring every task check and maximum 1/0.5 mm foot/body differences. These
are development cases for an asymmetric +3.75/-1.25 mm/s control envelope.
